/**
 * A label + href pair. Used for header navigation links and hero CTAs —
 * supplied by the page, which knows which sections it composed.
 */
export type LinkItem = {
  label: string;
  href: string;
};
